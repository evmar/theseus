[package]
name = "zig-hello-exe"
version = "0.1.0"
edition = "2024"

# [lib]
# crate-type = ["cdylib"]

[features]
wasm = ["runtime/wasm"]

[dependencies]
runtime = { workspace = true }
winapi = { workspace = true }
