This crate implements the host environment, for APIs like "create window" or
"play sound".

It has two implementations, either using SDL or web technologies. The Windows
API and DOS implementations are layered on top of it.
