[package]
name = "winapi"
version = "0.1.0"
edition = "2024"

[dependencies]
runtime = { workspace = true }
zerocopy = { version = "0.8.40", features = ["derive"] }
win32-derive = { path = "../derive" }
