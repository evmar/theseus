[workspace]
resolver = "2"
members = [
    "exe/winapi",
    "exe/zig_hello",
    "fc",
    "kernel32",
    "runtime",
]

[workspace.dependencies]
pe = { path = "../../win/rs/pe" }
runtime = { path = "runtime" }
kernel32 = { path = "kernel32" }

[profile.release]
debug = true
